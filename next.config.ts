import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Dummy/demo mode: no external image domains needed — all images are inline SVG/emoji.
  reactStrictMode: true,
};

export default nextConfig;
